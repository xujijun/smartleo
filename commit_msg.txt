feat: 优化测试语音停顿并扩充汉字测试库

1. 在 `useAudio` 中，调整 `speakHanziPrompt` 语音合成的输入，加入 `, ... ` 进行强制短暂但在听感上更自然的停顿（“请选择汉字... 水”）。
2. 在 `speakTestResult` 最后播报分数和鼓励语之间增加短暂停顿，让儿童在听到自己分数后有足够的间隔消化信息。
3. 建立专门的 `hanziTestList`（300 个精选儿童常见识字范围内的无重复汉字），取代原有学习模块中仅包含十几个字的数组，大幅增加汉字测试的难度和覆盖面，但每道题仍生成 9 个待选答案组成九宫格。

[请在此处粘贴 TAPD 需求/缺陷/任务 ID。获取方式：在 TAPD 页面点击「链接 -> 复制源码关键字」]
[如代码已全部提交，可在 TAPD ID 后添加这些指令来更新 TAPD 状态：#fix / #fixed / #finish / #finished / #close / #closed]
style: 测试页面倒计时与重听按钮合并至同行

1. 删除 `LetterTestView` 和 `NumberTestView` 中间原有的文字反馈区域 (`test-feedback` 和 `statusMessage` HTML渲染)，进一步精简页面。在保留原有语音反馈和选项变色反馈的基础上，释放更多垂直空间。
2. 修改 `styles.css`，将移动端的媒体查询中 `.test-status-row` 垂直排列的覆盖样式移除。
3. 让“剩余时间”倒计时卡片与“再听一次”按钮直接显示在同一行左右并排，使操作更加集中，适合儿童视线聚集。

[请在此处粘贴 TAPD 需求/缺陷/任务 ID。获取方式：在 TAPD 页面点击「链接 -> 复制源码关键字」]
[如代码已全部提交，可在 TAPD ID 后添加这些指令来更新 TAPD 状态：#fix / #fixed / #finish / #finished / #close / #closed]